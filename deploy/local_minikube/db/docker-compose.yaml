services:
  postgres_db:
    image: postgres:15
    container_name: external_postgres
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: mydb
    restart: always